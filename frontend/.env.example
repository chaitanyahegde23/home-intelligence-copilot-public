# Vite development uses /api through the local proxy by default.
VITE_API_BASE_URL=/api
# Client-side convenience limit; the API remains authoritative.
VITE_MAX_UPLOAD_SIZE_BYTES=5242880
# Client-side PDF guidance; keep aligned with MAX_DOCUMENT_SIZE_BYTES.
VITE_MAX_DOCUMENT_SIZE_BYTES=20971520
